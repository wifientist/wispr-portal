services:
  wispr-portal:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - PORT=${PORT:-8080}
    env_file:
      - .env
    volumes:
      - ./server.py:/app/server.py
      - ./index.html:/app/index.html
    restart: unless-stopped
